# rspec

### Basic
```zsh
  rspec --init
  rspec       # execute tests
  rspec rspec/teste_spec.rb
```

suffix *_spec.rb

File of the tests `spec/class_spec.rc`

```ruby
  describe Class do
    context "small description" do
      it "#nameMethd" do 
      end
    end 
  end
```
